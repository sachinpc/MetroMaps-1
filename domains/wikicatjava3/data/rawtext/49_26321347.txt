bean validation java bean validation jsr  is a framework that has been approved by the jcp as of  november  and is accepted as part of the java ee  specification bean validation defines a metadata model and api for javabean validation the metadata source is annotations with the ability to override and extend the metadata through the use of xml validation descriptors the hibernate team provides with hibernate validator the reference implementation of bean validation and also created the bean validation tck any implementation of jsr  needs to pass current version bean validation  jsr  improves upon the initial version  significant changes include final release of the  specification occurred 